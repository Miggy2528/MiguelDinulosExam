{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/^/(.*)\\.\\w+$", "dest": "/dist/$1" },
    { "src": "/^(.*)$", "dest": "/index.html" }
  ]
}